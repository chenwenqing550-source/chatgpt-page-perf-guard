{
  "name": "chatgpt-page-perf-guard",
  "version": "1.6.1",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check extension/core.js && node --check extension/monitor.js && node --check extension/popup.js"
  },
  "description": "Local-only ChatGPT page performance monitor and long-chat rendering guard.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
